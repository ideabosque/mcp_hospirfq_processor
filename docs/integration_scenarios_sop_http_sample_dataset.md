# Continuous Integration Scenarios SOP - MCP HospiRFQ Processor (Sample Dataset HTTP Transport — 中國大陸機位 B2B)

## 1. Document Control

| Field | Value |
|---|---|
| SOP title | MCP HospiRFQ Processor Live Integration SOP — Sample Dataset MCP HTTP Transport (中國大陸機位 B2B 動態) |
| Version | 0.1.0 |
| Owner / contact | assumed: project owner |
| Last updated | 2026-06-26 |
| Business domain | travel / hospitality RFQ — China-Taiwan airline seat B2B |
| Target environment | local staging-equivalent gateway using `mcp_hospirfq_processor/tests/.env` |
| Approval status | pending user confirmation |
| Companion SOPs | `integration_scenarios_sop_sample_dataset.md` (direct in-process transport, sample dataset); `integration_scenarios_sop_http.md` (MCP HTTP transport, general); `integration_scenarios_sop.md` (general in-process transport) |
| Test script | `mcp_hospirfq_processor/tests/run_http_integration.py` |
| Product data source | `../rfq_engine/rfq_engine/tests/prepare_test_data/sample_dataset/` |
| Non-product data source | `../rfq_engine/rfq_engine/tests/prepare_test_data/` |
| Source Excel | `2026大陸機位B2B動態.xlsx` (three sheets: 直飛機位動態、須轉機機位動態、不同點進出機位動態) |

## 2. Purpose and Scope

This SOP defines the ordered live integration scenarios used to validate `mcp_hospirfq_processor` through the **MCP HTTP transport layer** — the same JSON-RPC path (`initialize`, `tools/list`, `tools/call`) that a real AI agent uses to invoke tools via the gateway REST endpoint — using **real B2B airline seat data** derived from the Excel file `2026大陸機位B2B動態.xlsx`. All item names, airline names, city names, routes, fares, and cancellation policies are in **Chinese (Traditional)**.

Data is sourced from two directories:

- **Product data** (items, provider items, batches, price tiers, bundles, bundle components, cancellation policies, catalog refs, discount prompts) comes from `sample_dataset/` — generated from the real Excel file.
- **Non-product operational data** (segments, segment contacts, seeded requests, seeded quotes, seeded quote items, FX rates) comes from the parent `prepare_test_data/` directory — generated by the existing Faker-based prepare scripts.

The product dataset covers 33 flight routes (items), 42 provider items (airline offerings), 103 provider item batches (specific flight dates with seat counts), 126 item price tiers (adult / child / infant), 12 travel itinerary bundles, 44 bundle components, 1 cancellation policy, and 84 discount prompts (3 global + 6 segment + 33 item + 42 provider-item). The non-product dataset covers 3 segments with 15 contacts, 5 seeded requests, 9 seeded quotes, 5 seeded quote items, and 16 FX rates. All product prices are in **TWD**; non-product operational data uses **USD** as base currency.

Unlike the companion in-process SOP (which calls `MCPHospiRFQProcessor` methods directly in-process and configures GraphQL access), this SOP exercises only the `MCPHttpClient → gateway /mcp → dispatch_mcp → MCPHospiRFQProcessor` path. No GraphQL endpoints, `GraphQLClient` configuration, or `rfq_engine` imports are involved from the test script's perspective — the gateway handles backend dispatch internally.

- **In scope:** MCP JSON-RPC protocol (initialize, tools/list, tools/call), gateway MCP dispatch layer, `MCPHttpClient` async transport, SSE/JSON response parsing, Bearer token auth through `MCPHttpClient`, all 38 processor tools across 11 tool groups, Chinese (Traditional) product data validation.
- **Out of scope:** production testing, destructive cleanup of generated live test records, cloud provisioning, third-party production side effects, UI testing, load testing, direct in-process method calls (covered by companion in-process SOP), GraphQL endpoint configuration (handled internally by the gateway), data migration between backends, backend tuning/HA configuration.
- **System(s) under test:** `mcp_http_client.MCPHttpClient`, `silvaengine_gateway` REST MCP route (`/{endpoint_id}/mcp` with `Part-Id` request header), `mcp_daemon_engine` dispatch, `mcp_hospirfq_processor`.
- **Transport validation:** this SOP validates that the gateway correctly dispatches JSON-RPC `tools/call` requests to the MCP daemon engine, which instantiates and invokes `MCPHospiRFQProcessor` methods, serializes results into MCP `content` arrays, and returns them over HTTP. The test script never accesses GraphQL directly.

## 2.1 Controlling End-to-End Testing Procedure

The following procedure is authoritative for this sample-dataset MCP HTTP transport SOP:

1. Execute the end-to-end live integration testing with the script `mcp_hospirfq_processor/tests/run_http_integration.py`, which uses `MCPHttpClient` to drive all tool calls through the gateway REST/JSON-RPC MCP endpoint.
2. Use variables from `mcp_hospirfq_processor/tests/.env` to target the local integration instance. Do not hard-code credentials, endpoint IDs, partition IDs, gateway URLs, or `DATABASE_URL` values in generated reports.
3. Read and use the prepared data in `sample_dataset/` (product) and `prepare_test_data/` (non-product) as the reference dataset and dependency source for function inputs, expected relationships, and scenario ordering.
4. Before any tool group is executed, verify that `MCPHttpClient` can successfully complete the JSON-RPC `initialize` handshake with the gateway. This confirms the MCP transport layer is operational.
5. Optionally run with `--list-tools` to verify that `tools/list` returns the expected 38 registered tools before proceeding to `tools/call` scenarios.
6. Build the function dependency map before execution, then derive the test sequence priority from that dependency map rather than file-discovery order.
7. Perform end-to-end live integration testing in dependency order via async `tools/call` invocations.
8. Address any implementation, runner, data-contract, MCP transport, or scenario-ordering issues found during live execution.
9. Retest affected scenarios, then rerun the full dependency-ordered suite until all required calls pass with zero unexpected error responses.
10. Export the final per-function arguments and outputs into the project `docs/` directory as a dated results file.

## 3. Environment and Access

| Item | Value / source |
|---|---|
| Environment target | local gateway instance |
| MCP REST endpoint | `MCP_REST_URL` from `.env`, default `{GATEWAY_BASE_URL}/{endpoint_id}/mcp` |
| Credential source | `.env` variable names only; do not write secret values into reports |
| Auth flow | `MCPHttpClient` obtains JWT Bearer token from `{GATEWAY_BASE_URL}/auth/token` using `TOKEN_USERNAME` / `TOKEN_PASSWORD` (or uses `GATEWAY_TOKEN` if set); token is sent as `Authorization: Bearer *** header on all JSON-RPC requests |
| Required env vars (runner) | `base_dir`, `GATEWAY_BASE_URL`, `TOKEN_USERNAME`, `TOKEN_PASSWORD`, `GATEWAY_TOKEN`, `endpoint_id`, `part_id`, `MCP_REST_URL` (optional, derived by default) |
| Data stores | configured through local gateway (not accessed directly by the test script) |
| Messaging / events | none verified |
| Access constraints | local process access to gateway; `mcp_http_client` package must be importable (on `sys.path`) |
| Provisioning policy | manual approval required for new services or destructive cleanup |
| MCP client config | `base_url` = `MCP_REST_URL` (`/{endpoint_id}/mcp`), `bearer_token` = gateway JWT, `headers` = `{"Part-Id": PART_ID}` |
| Product data currency | TWD |
| Non-product data currency | USD (base), with FX rates for EUR, GBP, JPY, CNY, AUD, CAD, SGD, HKD |
| Dataset language | Chinese (Traditional) for all product item names, airline names, city names, bundle names, cancellation policy labels, and discount prompts; English for non-product operational data (segment names, request titles, quote notes) |

## 4. Dependency Readiness Requirements

| Dependency | Type | Health check | Required readiness | Owner |
|---|---|---|---|---|
| Python environment | infrastructure | import `MCPHttpClient` and `run_http_integration` module | operational | project owner |
| `mcp_http_client` package | library | importable on `sys.path` (added by script from `base_dir/mcp_http_client`) | operational | project owner |
| `silvaengine_gateway` local instance | internal | `POST /auth/token` returns JWT; `POST /mcp` JSON-RPC `initialize` returns protocol version | operational | project owner |
| MCP daemon engine configuration | internal | `tools/list` via `MCPHttpClient` returns 38 tools from `mcp_hospirfq_processor` module | loaded and operational | project owner |
| non-product data (segments, contacts, requests, quotes, quote items, FX rates) | test data | 3 segments, 15 contacts, 5 requests, 9 quotes, 5 quote items, 16 FX rates — all resolve via `tools/call` responses | initialized | project owner |
| product data (items, provider items, batches, price tiers, bundles, components, cancellation policies, catalog refs, discount prompts) | test data | 33 items, 42 provider items, 103 batches, 126 price tiers, 12 bundles, 44 components, 1 policy, 42 catalog refs, 84 discount prompts — all resolve via `tools/call` responses | initialized | project owner |
| catalog / KGE path | internal dependency | `tools/call inquire_catalog` returns payload without `errorCode` | operational | project owner |
| aiohttp | library | importable (required by `MCPHttpClient` for async HTTP) | installed | project owner |

## 5. Test Data Requirements

### 5.1 Product Data (from `sample_dataset/`)

| Asset type | Count | Source file | Notes / constraints |
|---|---|---|---|
| flight items (航班) | 33 prepared | `flight_products.json` | all item names in Chinese, e.g. `航班 TPE->HKG 經濟艙（桃園→香港）` |
| provider items (航空供應) | 42 prepared | `flight_products.json` | 10 airlines: 四川航空(3U)、中國國際航空(CA)、南方航空(CZ)、國泰航空(CX)、香港航空(HX)、廈門航空(MF)、長榮航空(BR)、中華航空(CI)、大彎區航空(GJ)、HB |
| provider item batches (航班批次) | 103 prepared | `flight_products.json` | specific flight dates with seat counts (`availabilityQty`); currency TWD; all linked to cancellation policy `fbd34345-…` |
| item price tiers (票價級距) | 126 prepared | `flight_products.json` | 3 pax types: `adult` (full price), `child` (75% of adult), `infant` (10% of adult); currency TWD |
| bundles (旅遊行程) | 12 prepared | `flight_products.json` | `CN-ITIN-001` through `CN-ITIN-012`; all names in Chinese |
| bundle components | 44 prepared | `flight_products.json` | `componentRole`: `flight_leg` |
| cancellation policies | 1 prepared | `flight_products.json` | `經濟艙 團體旅遊退票政策` — 出發前7天以上全額退款、出發前2-7天退款50%、出發前48小時內不予退款 |
| catalog refs | 42 matched | `flight_catalog_refs.json` | namespace `FLIGHTS` |
| discount prompts (折扣提示) | 84 prepared | `discount_prompts.json` | 3 global + 6 segment + 33 item + 42 provider-item; Chinese prompt text |

### 5.2 Non-Product Operational Data (from `prepare_test_data/`)

| Asset type | Count | Source file | Notes / constraints |
|---|---|---|---|
| segments | 3 prepared | `segments_and_contacts.json` | `King Group Tier`, `Gonzalez, Long and Ramos Tier`, `Dennis, Sosa and Jackson Tier` |
| segment contacts | 15 prepared | `segments_and_contacts.json` | 5 contacts per segment |
| seeded requests | 5 prepared | `requests.json` | scenarios: business_trip (×2), honeymoon, multi_leg, family_vacation |
| seeded quotes | 9 prepared | `quotes.json` | providers: AIRLINE-DL, AIRLINE-SQ, AIRLINE-CX; currency USD |
| seeded quote items | 5 prepared | `quote_items.json` | includes pax breakdown; some with subtotalDiscount |
| FX rates | 16 prepared | `fx_rates.json` | base USD → 8 target currencies + reverse |

### 5.3 Generated Data (per run)

| Asset type | Count | Notes / constraints |
|---|---|---|
| RFQ request | generated per run | runner creates a fresh request for mutable workflow tests via `tools/call submit_rfq_request` |
| quote / quote item | generated per run | quote item must include `pax_breakdown` (adult / child / infant) for `per_pax_type` pricing |
| installments | generated per run | setup quotes must have positive quote balance |

### 5.4 Load Order

**Product data:** run `sample_dataset/prepare_flight_products.py` → `sample_dataset/prepare_flight_catalog_refs.py` → `sample_dataset/prepare_discount_prompts.py`

**Non-product data:** run `prepare_test_data/prepare_segments_and_contacts.py` → `prepare_test_data/prepare_fx_rates.py` → `prepare_test_data/prepare_requests.py` → `prepare_test_data/prepare_quotes.py` → `prepare_test_data/prepare_quote_items.py`

**Full sequence:** MCP initialize handshake → (optional) tools/list verification → items → requests → quotes → pricing → installments → files → segments → availability → bundles → cancellation → catalog.

### 5.5 Reference Requirement

Before live execution, inspect `sample_dataset/flight_catalog_refs.json` (product) and `prepare_test_data/segments_and_contacts.json` (non-product) and verify that every function input used by the integration runner is backed by available prepared data or by a generated entity created earlier in the same run.

> **Note:** The `run_http_integration.py` script uses hardcoded sample UUIDs from prepared data. Unlike the in-process runner, it does not include a catalog-discovery-first gate. Catalog discovery can be added as a scenario extension if dynamic item selection is needed via `tools/call`.

## 6. Execution Order

```text
[initialize handshake] → items → requests → quotes → pricing → installments → files → segments → availability → bundles → cancellation → catalog
```

**Reason for ordering:** this project is an RFQ processor facade; item/provider validation must precede request creation, request/provider assignment must precede quote creation, quote pricing must precede installment validation, and bundle/policy/catalog readbacks are reference checks. The MCP `initialize` handshake is implicit — `MCPHttpClient.__aenter__` calls `initialize()` automatically before any `tools/call`.

**Sequence construction rule:** the execution order must be rebuilt or revalidated before each certification run from the actual function dependencies in the runner and codebase. Static file order is not sufficient.

## 7. Integration Scenarios

> Priority drives execution when time is limited (P0 = must pass to certify; P1 = must pass to certify; P2 = should pass or be documented as skipped).

### Scenario INT-SDMCP-000 — MCP transport initialization and tool discovery (sample dataset)

| Field | Value |
|---|---|
| **ID** | INT-SDMCP-000 |
| **Name** | MCP transport initialization and tool discovery (sample dataset) |
| **Priority** | P0 |
| **Type** | MCP protocol / transport |
| **CI trigger** | manual / pre-release |
| **Preconditions** | Gateway is running; `mcp_hospirfq_processor` is loaded into MCP daemon engine |
| **Dependencies** | gateway, mcp_daemon_engine, mcp_hospirfq_processor module |
| **Test data** | none (protocol handshake only) |
| **Steps** | run `run_http_integration.py --list-tools`; verify `initialize` JSON-RPC handshake completes; verify `tools/list` returns 38 tools; verify tool names match `mcp_configuration.py` |
| **Expected behavior** | `MCPHttpClient` enters async context, sends `initialize`, receives protocol version `2024-11-05`; `tools/list` returns all 38 registered tool names with descriptions and input schemas |
| **Validation points** | protocol version, tool count (38), tool names match configuration |
| **Cross-system checks** | gateway MCP dispatch correctly resolves `mcp_hospirfq_processor` module and class |

### Scenario INT-SDMCP-001 — Flight item discovery via tools/call (sample dataset)

| Field | Value |
|---|---|
| **ID** | INT-SDMCP-001 |
| **Name** | Flight item discovery via tools/call (sample dataset) |
| **Priority** | P1 |
| **Type** | MCP tools/call |
| **CI trigger** | manual / pre-release |
| **Preconditions** | MCP transport initialized (INT-SDMCP-000); product data loaded (33 items, 42 provider items, 103 batches) |
| **Dependencies** | gateway, INT-SDMCP-000 |
| **Test data** | prepared flight item UUID (e.g. `28ca431e-…` for 航班 TPE->HKG 經濟艙 桃園→香港), item type `flight` |
| **Steps** | `tools/call search_items` with `item_type=flight`; `tools/call get_item` with prepared `item_uuid`; `tools/call get_provider_items` with prepared `item_uuid` |
| **Expected behavior** | all three `tools/call` invocations return MCP `content` arrays with decoded JSON payloads; item names in Chinese (Traditional); provider item `itemSpec` contains `airline_name_zh`, `cabin_class_zh`, `origin_city_zh`, `destination_city_zh`; no JSON-RPC error envelopes |
| **Validation points** | item list total (≥33), item detail (Chinese name), provider item detail (airline code, Chinese airline name, base price in TWD), batch list (batch numbers with Chinese destination city, availability qty), MCP content parsing |
| **Cross-system checks** | returned partition matches `.env` endpoint/part |

### Scenario INT-SDMCP-002 — RFQ request lifecycle via tools/call (sample dataset)

| Field | Value |
|---|---|
| **ID** | INT-SDMCP-002 |
| **Name** | RFQ request lifecycle via tools/call (sample dataset) |
| **Priority** | P1 |
| **Type** | workflow / MCP tools/call |
| **CI trigger** | manual / pre-release |
| **Preconditions** | Flight item exists (INT-SDMCP-001); non-product data loaded (segments, contacts) |
| **Dependencies** | item data, request mutations |
| **Test data** | generated RFQ request via `tools/call submit_rfq_request` (Chinese request title, e.g. `2026夏季中國大陸旅遊團體機位需求`) |
| **Steps** | `tools/call submit_rfq_request`; `tools/call get_rfq_request`; `tools/call search_rfq_requests`; `tools/call update_rfq_request`; `tools/call add_item_to_rfq_request`; `tools/call remove_item_from_rfq_request`; `tools/call assign_provider_item_to_request_item`; `tools/call remove_provider_item_from_request_item`; `tools/call assign_provider_item_to_request_item` (for quote workflow) |
| **Expected behavior** | all `tools/call` invocations return MCP content with decoded business payloads; request mutations succeed and final request has provider assignment |
| **Validation points** | request UUID, status transitions, item array, provider item assignment, MCP content text decoding |
| **Cross-system checks** | provider item belongs to the selected item |

### Scenario INT-SDMCP-003 — Quote lifecycle via tools/call (sample dataset)

| Field | Value |
|---|---|
| **ID** | INT-SDMCP-003 |
| **Name** | Quote lifecycle with per-passenger quote item via tools/call (sample dataset) |
| **Priority** | P1 |
| **Type** | workflow / MCP tools/call |
| **CI trigger** | manual / pre-release |
| **Preconditions** | Generated request has provider item assignment (INT-SDMCP-002); `pax_breakdown` includes adult / child / infant |
| **Dependencies** | request workflow, quote mutations, price tier rules |
| **Test data** | generated request/quote/quote item; `pax_breakdown` with adult, child, infant counts for `per_pax_type` pricing |
| **Steps** | `tools/call confirm_request_and_create_quotes`; `tools/call get_quote`; `tools/call search_quotes`; `tools/call update_quote`; `tools/call update_quote_item` |
| **Expected behavior** | all `tools/call` invocations succeed; quote reaches updateable state and quote item accepts discount |
| **Validation points** | quote UUID, quote item UUID, final subtotal (TWD), MCP content payload structure |
| **Cross-system checks** | quote item pricing uses provider item `basePricePerUom` × pax type multiplier (adult=1.0, child=0.75, infant=0.10) |

### Scenario INT-SDMCP-004 — Pricing and installment workflow via tools/call (sample dataset)

| Field | Value |
|---|---|
| **ID** | INT-SDMCP-004 |
| **Name** | Pricing and installment workflow via tools/call (sample dataset) |
| **Priority** | P1 |
| **Type** | workflow / MCP tools/call |
| **CI trigger** | manual / pre-release |
| **Preconditions** | Quote exists with positive balance (INT-SDMCP-003) |
| **Dependencies** | pricing, quote, installment mutations |
| **Test data** | generated setup quotes and installments |
| **Steps** | `tools/call get_item_price_tiers` (126 prepared tiers); `tools/call get_discount_prompts` (84 prepared prompts in Chinese); `tools/call calculate_quote_pricing`; `tools/call confirm_quote_and_create_installments`; `tools/call get_installments`; `tools/call create_installment`; `tools/call create_installments`; `tools/call update_installment` |
| **Expected behavior** | all pricing and installment `tools/call` invocations succeed; discount prompts returned with Chinese `discountPrompt` text |
| **Validation points** | installment UUIDs, amount fields (TWD), status updates, MCP content decoding |
| **Cross-system checks** | installment totals do not exceed quote balance; price tier amounts match `flight_products.json` (adult=basePricePerUom, child=75%, infant=10%) |

### Scenario INT-SDMCP-005 — Reference and support APIs via tools/call (sample dataset)

| Field | Value |
|---|---|
| **ID** | INT-SDMCP-005 |
| **Name** | Reference and support APIs via tools/call (sample dataset) |
| **Priority** | P2 |
| **Type** | MCP tools/call |
| **CI trigger** | manual / pre-release |
| **Preconditions** | Prepared product reference data exists (12 bundles, 44 bundle components, 1 cancellation policy, 42 catalog refs); non-product data loaded (3 segments, 15 contacts) |
| **Dependencies** | file, segment, bundle, cancellation, catalog resolvers |
| **Test data** | seeded request, segment (`323dec2b-…` from product data; `5101fc1b-…`, `7f033a6f-…` from non-product data), 12 bundles (`CN-ITIN-001` through `CN-ITIN-012`), cancellation policy (`經濟艙 團體旅遊退票政策`), catalog refs (namespace `FLIGHTS`) |
| **Steps** | `tools/call upload_rfq_file`; `tools/call get_rfq_files`; `tools/call get_segment_contacts` (15 contacts across 3 segments); `tools/call search_bundles` (12 bundles with Chinese names); `tools/call get_bundle`; `tools/call search_bundle_components` (44 components, `flight_leg`); `tools/call get_cancellation_policy` (`經濟艙 團體旅遊退票政策`); `tools/call search_cancellation_policies`; `tools/call inquire_catalog` |
| **Expected behavior** | all `tools/call` invocations return successful MCP content payloads; bundle names in Chinese; cancellation policy label in Chinese; catalog payload has no `errorCode` |
| **Validation points** | file list, segment contact list (15 contacts), bundle components, policy tiers (168h=100%, 48h=50%, 0h=0%), catalog results, MCP content text decoding |
| **Cross-system checks** | catalog result namespace is `FLIGHTS`; segment contacts match `segments_and_contacts.json` |

### Scenario INT-SDMCP-006 — Availability hold lifecycle via tools/call (sample dataset)

| Field | Value |
|---|---|
| **ID** | INT-SDMCP-006 |
| **Name** | Availability hold lifecycle via tools/call (sample dataset) |
| **Priority** | P1 |
| **Type** | workflow / MCP tools/call |
| **CI trigger** | manual / pre-release |
| **Preconditions** | Provider item batch has capacity in the active backend |
| **Dependencies** | availability query/mutations |
| **Test data** | prepared provider item and batch; select highest-capacity batch (e.g. `CA418-20260714-成都`, `availabilityQty=20`, `serviceStartAt=2026-07-14T18:10:00`) |
| **Steps** | `tools/call check_availability`; `tools/call acquire_availability_hold`; `tools/call confirm_availability_hold`; `tools/call acquire_availability_hold` (for release); `tools/call release_availability_hold`; `tools/call acquire_availability_hold` (for expire); `tools/call expire_availability_hold` |
| **Expected behavior** | check/acquire/confirm/release succeed; immediate expire returns expected live no-op because TTL has not elapsed |
| **Validation points** | hold token extraction from MCP content, status/reason payloads, available quantity |
| **Cross-system checks** | capacity changes remain coherent across hold lifecycle |

> **Note:** Select the highest-capacity matching batch for hold lifecycle tests. The sample dataset's top-capacity batches are `CA418-*` (20 seats each) under provider item `9a1a5a90-…` (航班 TPE->TFU 經濟艙 桃園→成都, 中國國際航空). Prior cert runs' confirmed holds may exhaust low-capacity batches (e.g. `3U3982-*` with 9 or 0 seats), causing false failures.

## 8. Failure and Resilience Scenarios

| Scenario | Injected fault | Expected behavior |
|---|---|---|
| missing gateway | stop gateway before live execution | `MCPHttpClient` connection fails; `_get_gateway_token` raises `ConnectionError`; script exits with FATAL message |
| invalid credentials | bad auth env values | `/auth/token` returns 401; token request fails; no tests execute |
| MCP daemon not loaded | `mcp_hospirfq_processor` not loaded into daemon engine | `tools/list` returns 0 tools or `tools/call` returns JSON-RPC error with method-not-found |
| stale seeded IDs | prepared sample dataset IDs missing | affected `tools/call` returns MCP content with in-band error envelope |
| MCP content parse failure | gateway returns non-JSON text in MCP content | `_parse_mcp_content` returns raw string; result tracking records it |
| SSE response instead of JSON | gateway returns `text/event-stream` | `MCPHttpClient._parse_sse_response` extracts JSON-RPC payload from SSE data lines |
| premature hold expiry | expire a fresh 15-minute hold | expected no-op envelope is recorded via `tools/call expire_availability_hold` |
| batch capacity exhausted | select a batch with `availabilityQty=0` (e.g. `3U3982-20260628-成都`) | availability check returns zero; hold acquire fails with clear capacity error |

## 9. Data Reconciliation Checks

| Check | Rule | Tolerance |
|---|---|---|
| Generated request linkage | quote request UUID equals generated request UUID | 0 mismatches |
| Quote item linkage | quote item quote UUID equals generated quote UUID | 0 mismatches |
| Price tier consistency | price tier amounts match `flight_products.json`: adult = `basePricePerUom`, child = 75%, infant = 10% | amount: 0.01 TWD |
| Pricing consistency | final subtotal reflects subtotal discount | amount: 0.01 TWD |
| Installment consistency | created installments fit quote balance | amount: 0.01 TWD |
| Catalog consistency | catalog namespace equals `FLIGHTS` and payload has results | 0 missing |
| Item name language | returned item names are Chinese (Traditional), matching `flight_products.json` | 0 mismatches |
| Discount prompt language | returned discount prompt text is Chinese (Traditional) | 0 mismatches |
| Bundle name language | returned bundle names are Chinese (Traditional) | 0 mismatches |
| Cancellation policy | policy label is `經濟艙 團體旅遊退票政策`; tiers match (168h=100%, 48h=50%, 0h=0%) | 0 mismatches |
| Segment contacts | returned contacts match `segments_and_contacts.json` from `prepare_test_data/` | 0 mismatches |
| Error envelope check | no unexpected top-level `error` or in-band `error_code` in MCP content | 0 unexpected |
| MCP content structure | every `tools/call` response has a `content` array with at least one `text` item | 0 missing |
| Tool count verification | `tools/list` returns exactly 38 tools matching `mcp_configuration.py` | 0 mismatches |

## 10. Entry and Exit Criteria

**Entry criteria (testing may begin when):**
- This SOP is approved by the user.
- Local gateway is running and reachable.
- `mcp_hospirfq_processor` is loaded into the MCP daemon engine (verified via `tools/list`).
- `.env` names are configured (`GATEWAY_BASE_URL`, `TOKEN_USERNAME`, `TOKEN_PASSWORD`, `endpoint_id`, `part_id`).
- Product data prepare scripts have been run: `sample_dataset/prepare_flight_products.py` → `sample_dataset/prepare_flight_catalog_refs.py` → `sample_dataset/prepare_discount_prompts.py`.
- Non-product data prepare scripts have been run: `prepare_test_data/prepare_segments_and_contacts.py` → `prepare_test_data/prepare_fx_rates.py` → `prepare_test_data/prepare_requests.py` → `prepare_test_data/prepare_quotes.py` → `prepare_test_data/prepare_quote_items.py`.
- Authentication succeeds (`/auth/token` returns JWT).
- `MCPHttpClient` can complete `initialize` JSON-RPC handshake.
- No destructive cleanup is required.

**Exit criteria (certification may be issued when):**
- All P0 and P1 scenarios (INT-SDMCP-000 through INT-SDMCP-004, INT-SDMCP-006) pass.
- INT-SDMCP-005 passes or is documented as skipped with rationale.
- `tools/list` returns all 38 expected tools.
- No unexpected error responses remain.
- Any defects or data-contract issues found during execution have been fixed and retested.
- The final full dependency-ordered live suite has passed after the last fix.
- Per-call function results are exported to `docs/` as a dated results file.
- Any expected live no-op behavior is explicitly documented.
- Open environment warnings are listed as non-blocking or resolved.

## 11. CI Trigger and Cadence

| Trigger | Scope run | Required to pass |
|---|---|---|
| Manual local validation | full live suite via `run_http_integration.py` with sample dataset | yes for certification |
| Pre-release | full live suite plus report export (`--export`) | yes |
| Dataset refresh | whenever `2026大陸機位B2B動態.xlsx` or prepare scripts change, rerun prepare scripts + full suite | yes |
| Pull request | assumed: unit tests plus optional smoke subset (`--only items,requests`) | assumed |
| Nightly | assumed: full suite against isolated test tenant | assumed |
| MCP transport change | full live suite plus `--list-tools` verification | yes |

## 12. Reporting and Certification Expectations

- **Report format:** Markdown.
- **Report artifact:** `docs/test_results/http_integration_results_sample_dataset_<YYYYMMDD>.md` (dated) with per-function tool name, MCP method (`tools/call`), arguments, decoded MCP content payload, elapsed time, pass/error/fail status.
- **Required certification decision:** `Integration Certified`, `Ready for UAT`, `Ready for Production`, `Ready with Conditions`, or `Not Ready`.
- **Distribution:** assumed: project owner / release owner.
- **Required content:** per-function tool name, MCP method (`tools/call`), arguments, decoded MCP content payload, elapsed time, pass/error/fail status.
- **Additional transport-level evidence:** `initialize` protocol version, `tools/list` tool count and names, any SSE-vs-JSON response observations.
- **Dataset labeling:** every report must state both the product data source (`sample_dataset/` / `2026大陸機位B2B動態.xlsx`) and non-product data source (`prepare_test_data/`).
- **Backend labeling:** every report must state which backend was used via `silvaengine_gateway` and must not be conflated with a different backend or dataset certification.

## 13. Comparison with Companion SOPs

| Aspect | In-process SOP (`integration_scenarios_sop_sample_dataset.md`) | This SOP (`integration_scenarios_sop_http_sample_dataset.md`) |
|---|---|---|
| Test script | `run_integration.py` | `run_http_integration.py` |
| Transport | direct in-process method calls on `MCPHospiRFQProcessor` | `MCPHttpClient` async HTTP JSON-RPC through gateway `/mcp` |
| MCP protocol exercised | no | yes (`initialize`, `tools/list`, `tools/call`) |
| Auth | `GraphQLClient.get_gateway_token()` for in-process GraphQL | `MCPHttpClient` Bearer token from `/auth/token` (no GraphQL access) |
| Catalog discovery gate | mandatory first step (INT-SD-000) | optional (not included in runner by default) |
| Scenario IDs | INT-SD-000 … INT-SD-006 | INT-SDMCP-000 … INT-SDMCP-006 |
| Report artifact | `docs/test_results/live_integration_results_sample_dataset_<date>.md` | `docs/test_results/http_integration_results_sample_dataset_<date>.md` |
| Execution model | synchronous | asynchronous (`asyncio`) |
| Tool groups | 12 (includes catalog_discovery) | 11 (items through catalog) |
| Additional validation | — | MCP transport layer, JSON-RPC dispatch, content parsing |
| Product data source | `sample_dataset/` | `sample_dataset/` (same) |
| Non-product data source | `prepare_test_data/` | `prepare_test_data/` (same) |
| Dataset language | Chinese (Traditional) | Chinese (Traditional) (same) |
| Currency | TWD (product), USD (non-product) | TWD (product), USD (non-product) (same) |

## 14. Dataset Reference

### 14.1 Source Files — Product Data (`sample_dataset/`)

| File | Content |
|---|---|
| `2026大陸機位B2B動態.xlsx` | Original B2B Excel: 3 sheets (直飛機位動態、須轉機機位動態、不同點進出機位動態) |
| `flight_products.json` | 33 items, 42 provider items, 103 batches, 126 price tiers, 12 bundles, 44 bundle components, 1 cancellation policy |
| `flight_catalog_refs.json` | 33 ingested + 42 matched catalog refs (namespace `FLIGHTS`) |
| `discount_prompts.json` | 84 discount prompts (3 global + 6 segment + 33 item + 42 provider-item) |
| `prepare_flight_products.py` | Reads Excel → creates items, provider items, batches, price tiers, bundles, bundle components, cancellation policy |
| `prepare_flight_catalog_refs.py` | Creates catalog refs from KGE ingestion results |
| `prepare_discount_prompts.py` | Creates discount prompts (global, segment, item, provider-item scopes) |

### 14.2 Source Files — Non-Product Data (`prepare_test_data/`)

| File | Content |
|---|---|
| `segments_and_contacts.json` | 3 segments, 15 contacts (5 per segment) |
| `requests.json` | 5 seeded requests (business_trip ×2, honeymoon, multi_leg, family_vacation) |
| `quotes.json` | 9 seeded quotes; some with displayCurrency + locked FX rate |
| `quote_items.json` | 5 seeded quote items with pax breakdown; 4 empty quotes; 10 skipped |
| `fx_rates.json` | 16 FX rates (USD ↔ EUR, GBP, JPY, CNY, AUD, CAD, SGD, HKD) |

### 14.3 Airlines (航空公司)

| IATA | English | Chinese |
|---|---|---|
| 3U | Sichuan Airlines | 四川航空 |
| CA | Air China | 中國國際航空 |
| CI | China Airlines | 中華航空 |
| CX | Cathay Pacific | 國泰航空 |
| CZ | China Southern Airlines | 南方航空 |
| GJ | Loong Air | 大彎區航空 |
| HB | (variant) | 大彎區航空 |
| HX | Hong Kong Airlines | 香港航空 |
| MF | Xiamen Airlines | 廈門航空 |
| BR | EVA Air | 長榮航空 |

### 14.4 Routes (航線)

33 unique routes covering Taiwan (TSA 松山、TPE 桃園、KHH 高雄) ↔ China (TFU 成都、CKG 重慶、HGH 杭州、PVG 上海浦東、PEK 北京、FOC 福州、CAN 廣州、HKG 香港、SZX 深圳、KMG 昆明、WUH 武漢、HRB 哈爾濱、YIH 宜昌、AKH 阿克蘇).

### 14.5 Pricing Structure

- **Currency:** TWD (product data)
- **Pax types:** `adult` (100% of `basePricePerUom`), `child` (75%), `infant` (10%)
- **Pricing mode:** `per_pax_type`
- **UOM:** `seat`
- **Availability mode:** `require_hold`

### 14.6 Cancellation Policy

| Label | 經濟艙 團體旅遊退票政策 |
|---|---|
| Description | B2B航空機位團體旅遊標準退票政策。出發前7天以上取消全額退款；出發前2-7天取消退款50%；出發前48小時內取消不予退款。 |
| Tier 1 | ≥ 168 hours before departure → 100% refund |
| Tier 2 | ≥ 48 hours before departure → 50% refund |
| Tier 3 | ≥ 0 hours before departure → 0% refund |

### 14.7 Bundles (旅遊行程)

12 multi-city itinerary bundles (`CN-ITIN-001` through `CN-ITIN-012`), each composed of 2-5 flight legs (`componentRole=flight_leg`). Example: `CN-ITIN-003: 中國大陸旅遊行程 桃園→香港 + 香港→成都 + 成都→香港 + 香港→桃園`.

### 14.8 Segments (non-product)

| Segment UUID | Name | Contacts |
|---|---|---|
| `5101fc1b-…` | King Group Tier | 5 |
| `7f033a6f-…` | Gonzalez, Long and Ramos Tier | 5 |
| `323dec2b-…` | Dennis, Sosa and Jackson Tier | 5 |

### 14.9 FX Rates (non-product)

| Base | Targets | Providers |
|---|---|---|
| USD | EUR, GBP, JPY, CNY, AUD, CAD, SGD, HKD (16 rates with reverse) | Reuters Refinitiv, Bloomberg FX, OANDA, ECB Reference, XE Currency Data, Wise Mid-Market, Tenant Treasury Desk |

## 15. Sign-off

| Role | Name | Date | Decision |
|---|---|---|---|
| Test owner | project owner | pending | pending user confirmation of this SOP |
| Release manager | pending | pending | pending |